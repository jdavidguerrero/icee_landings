node_modules/
.next/
.turbo/
out/
dist/
.env
.env.local
.env.*.local
*.tsbuildinfo
.DS_Store

# Explicit per-app build output (belt + suspenders)
apps/corporate/.next/
apps/corporate/.turbo/
apps/product/.next/
apps/product/.turbo/

# Large binary assets — serve from CDN, not git
*.mov
*.webm
*.avi
packages/assets/

# Corporate videos are large (13–45 MB) — CDN only
apps/corporate/public/videos/

# Product videos — small (≤2 MB), commit for Vercel deployment
# Exclude all mp4s globally, then re-include the small product ones
*.mp4
!apps/product/public/videos/icee_pos_1.mp4
!apps/product/public/videos/icee_pos_2.mp4
